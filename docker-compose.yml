services:
  backend-service:
    build: ./service-backend
    ports:
      - "8040:8040"
    depends_on:
      - kundali-service
    environment:
      DEPLOYMENT_ENV: local
      AWS_DEFAULT_REGION: us-east-2
    volumes:
      - ~/.aws:/root/.aws:ro

  kundali-service:
    build: ./service-kundali
    ports:
      - "8000:8000"